-- Токен запуска из хаба GG: приезжает в startapp при открытии игры из лаунчера,
-- нужен, чтобы честно рапортовать исход матча (см. GG/SDK.md §2.6). NULL —
-- игрок пришёл напрямую в бота, мимо хаба: рапортовать нечем и не нужно.
ALTER TABLE users ADD COLUMN gg_launch TEXT;
